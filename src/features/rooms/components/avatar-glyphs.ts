import type { AvatarType } from '../types'

export const AVATAR_GLYPHS: Record<AvatarType, string> = {
  shadow:   '◆',
  ember:    '◈',
  tide:     '◉',
  void:     '◍',
  revenant: '◇',
  still:    '○',
}
